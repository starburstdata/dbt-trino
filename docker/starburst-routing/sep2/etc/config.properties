coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080

# Register with the Control Plane instead of a standalone discovery service.
discovery.uri=http://portal:8080
internal-communication.http2.enabled=false
http-server.process-forwarded=true

web-ui.shared-secret=secret
# Must match the sharedSecret used to register this backend with the Portal.
internal-communication.shared-secret=internal-shared-secret
